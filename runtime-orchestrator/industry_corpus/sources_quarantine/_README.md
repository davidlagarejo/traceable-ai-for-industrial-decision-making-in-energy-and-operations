# Quarantined sources

These source YAMLs reference URLs that returned errors (404, oversize, etc.)
during the V10 P0 F6 seed. They're kept here for audit (so we don't repeat
them) but are NOT ingested by ETL because they're outside `sources/`.


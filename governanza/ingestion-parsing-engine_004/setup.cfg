[metadata]
name = zlab-ingestion-parsing-engine
version = 0.1.0
description = Ingestion and parsing engine for ZLab.

[options]
packages = find:
python_requires = >=3.9

[options.packages.find]
include =
    ingestion_parsing_engine
    ingestion_parsing_engine.*

[metadata]
name = zlab-canonical-normalization-engine
version = 0.1.0
description = Canonical normalization engine for ZLab.

[options]
packages = find:
python_requires = >=3.9

[options.packages.find]
include =
    canonical_normalization_engine
    canonical_normalization_engine.*

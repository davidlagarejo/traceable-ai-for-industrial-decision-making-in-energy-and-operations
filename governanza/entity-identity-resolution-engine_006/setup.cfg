[metadata]
name = zlab-entity-identity-resolution-engine
version = 0.1.0
description = Entity identity and resolution engine for ZLab.

[options]
packages = find:
python_requires = >=3.9

[options.packages.find]
include =
    entity_identity_resolution_engine
    entity_identity_resolution_engine.*

# Operational Intelligence Skill Execution Backlog — Latest

Produced at: 2026-05-07

## Current truth

This backlog now records a **closed bounded runtime-authority phase**
plus the residual optional deepening lanes that were intentionally not
made blocking.

It does **not** reopen or weaken any of the following closures:

- `dynamic_congruence_prompt_completion_backlog_latest.md`
- `dynamic_congruence_prompt_closure_refresh_latest.md`
- `dynamic_congruence_phase2_architecture_closure_latest.md`
- `dynamic_congruence_phase2_execution_backlog_latest.md`
- `dynamic_congruence_browser_lane_operational_certification_latest.md`
- `dynamic_congruence_browser_lane_family_expansion_latest.md`

Current executable truth remains:

- suite: `runtime-orchestrator`
- date: `2026-05-07`
- command: `pytest -q`
- result: `739 passed, 15 warnings`

## New deepening references

The next phase is now formalized in:

- `autonomous_research_loop_master_plan_latest.md`
- `autonomous_research_loop_execution_backlog_latest.md`
- `research_brain_100_percent_completion_plan_latest.md`
- `research_brain_100_percent_completion_backlog_latest.md`
- `report_98_readiness_closure_plan_latest.md`

These documents define the complete plan/backlog for:

1. autonomous research execution
2. `50+` latent-combination floor enforcement
3. deeper multi-source coverage
4. lower-friction excerpt capture
5. closed-loop atom refresh and reranking
6. the final remaining work to claim `100%` of the stronger expanded research-brain target

There is now also a separate closure track for final client-facing report quality:

7. take the rendered report system to `98/100` strategic readiness for funds, banks, utilities, operators, and diligence users without weakening epistemic governance

That `98/100` lane is no longer plan-only:

- the first `Wave 1` runtime slice is now live, adding
  `thesis_constellation_register`,
  `evidence_pack_register`,
  and
  `visible_claim_integrity_register`
  to the executive-thesis / report-summary chain.
- the second `Wave 1` runtime slice is now also live in the rendered report body:
  `motor_016` now surfaces `Rival Thesis Constellation`, `Differentiated Evidence Packs`,
  and a wider visible `Strategic Gold Nugget Set` inside `Executive Structural Thesis`,
  so rival hypotheses and discriminator packs are no longer payload-only metadata.
- the third `Wave 1` runtime slice is now also live across adjacent sections:
  `motor_016` now binds `Financial Exposure Under Uncertainty`, `Peer / Competitive Comparison`,
  and visible `TAD` rows back to the same pack/constellation logic, so those sections no longer
  read like parallel templates detached from the thesis spine.
- the first `Wave 2` runtime slice is now also live:
  `competitive_comparison.py` now emits peer requirements, candidate peer frames, better-practice
  deltas, and an explicit peer-superiority block reason, and `motor_016` now renders those
  structures visibly inside `Peer / Competitive Comparison`.
- the first `Wave 4` runtime slice is now also live:
  `motor_018` now emits a sovereign `chart_strategic_value_register` plus per-chart
  `strategic_value_score`, `strategic_value_tier`, and `strategic_value_reason`, and chart
  emission order is now driven by strategic value instead of only by construction order.
- the second `Wave 4` runtime slice is now also live:
  `motor_016` now applies a strategic-surface gate before attaching charts to body sections, so
  `decorative_risk` charts are suppressed from the primary report surface once enough
  `thesis_critical` / `strategic_support` charts already carry the thesis, and the package now
  emits `chart_strategic_surface_policy_register` plus `chart_strategic_surface_summary`.
- the first `Wave 3` runtime slice is now also live:
  `gold_nuggets.py` now emits richer nugget-theme / strength / selection-priority metadata,
  `executive_thesis.py` now selects visible nuggets by diversified theme-aware strength rather
  than simple truncation order, and the visible thesis surface now widens to a bounded `8`
  nuggets instead of a hard `5`.
- the second `Wave 3` runtime slice is now also live:
  `strategic_tad.py` now emits decision-front / trigger-family / financial-exposure /
  evidence-pack-family / no-go-class fields, `executive_thesis.py` now preserves that richer
  action structure into `top_actions`, and `motor_016` now renders TAD as a decision-front map
  instead of only an action/status list.
- the third `Wave 3` runtime slice is now also live:
  `congruence_engine.py` now ranks contradiction rows by `correlation_constellation_score` and
  carries supporting-correlation / pressure sidecars, `executive_thesis.py` now emits a bounded
  `correlation_constellation_register` with a governed fallback when the upstream graph is sparse,
  and `motor_016` now renders `Correlation Constellation Signals` directly inside the visible
  thesis instead of leaving multi-layer reinforcement as payload-only structure.
- the next report-quality slice is now also live at the `Wave 4 -> Wave 5` boundary:
  `motor_016._build_case_adaptation_memo(...)` now emits a `diversity_register`,
  `diversity_score`, and `diversity_failure`, compares cases using richer fingerprint dimensions
  including `decision_front_statuses` and `bottlenecks`, and blocks template reuse even when the
  only visible divergence from a close reference is low-signal rather than structurally meaningful.
- the remaining `Wave 4` chart closure slice is now also live:
  `motor_018.py` now emits explicit contradiction / hypothesis / nugget bindings per chart, and
  `motor_016._apply_chart_strategic_surface_gate(...)` now demotes `decorative_risk` body charts
  to a governed appendix surface when they still have bounded support value instead of only suppressing them.
- the first `Wave 5` report-surface slice is now also live:
  `motor_016` now applies a governed section-density gate after empty-section fallback, emits
  `section_density_surface_policy_register` plus `section_density_surface_summary`, and can demote
  thin support sections to appendix without touching the protected strategic spine or violating the
  active render contract / visible outline.
- the second `Wave 5` report-surface slice is now also live:
  `motor_016` now emits section-level strategic surface scoring
  (`section_surface_strategic_profile` / `section_surface_strategic_state`) plus
  `section_strategic_surface_policy_register` / `section_strategic_surface_summary`, and can demote
  populated but strategically optional sections to appendix only when the body already has enough
  high-value sections and the protected / contract-required surface remains intact.
- the third `Wave 5` report-surface slice is now also live:
  `motor_016` now emits section-level redundancy governance
  (`section_surface_redundancy_profile` / `section_surface_redundancy_state`) plus
  `section_redundancy_surface_policy_register` / `section_redundancy_surface_summary`, and can demote
  strategically optional sections to appendix when they materially overlap the retained thesis spine,
  while still respecting the protected / contract-required surface and the minimum body floor.
- the fourth `Wave 5` report-density slice is now also live inside the thesis core:
  `executive_thesis.py` now applies concept-marker-aware semantic compaction across
  `top_gold_nuggets`, `thesis_constellation_register`, `evidence_pack_register`, and
  `correlation_constellation_register`, so repeated strategic ideas no longer survive as paraphrased
  duplicates while legitimate `challenger_hypothesis` lanes remain protected from marker-only collapse.
- the fifth `Wave 5` report-surface slice is now also live in the rendered thesis body:
  `motor_016.py` now emits `thesis_surface_compaction_register` plus
  `thesis_surface_compaction_summary`, compacts rendered nugget / constellation / evidence-pack /
  correlation repetition against the visible thesis spine, preserves a bounded primary nugget lane,
  and protects `challenger_hypothesis` plus distinct correlation rows from collapsing just because
  related tariff or denominator language is already visible elsewhere in the same thesis.
- the sixth `Wave 5` report-tone slice is now also live in the rendered thesis body:
  `motor_016.py` now emits a governed `Strategic Reading` layer plus
  `thesis_surface_readout_register`, so the visible `Executive Structural Thesis` opens with a
  bounded senior-operator readout (`Framing Risk`, `Dominant Variable Shift`,
  `Capital-at-Risk Logic`, `Comparison / Boundary Warning`, `Minimum Evidence Pivot`) rather than
  reading only as a correct but field-like inventory of thesis metadata.
- the seventh `Wave 5` report-tone slice is now also live across adjacent body sections:
  `motor_016.py` now emits `section_surface_readout_register` and opens
  `Financial Exposure Under Uncertainty`, `Peer / Competitive Comparison`, and
  `TAD — Immediate Action Priority` with bounded strategic readouts
  (`Strategic Reading`, `Comparison Reading`, `Decision Reading`) so the PDF sustains the same
  senior-operator voice outside the thesis itself instead of dropping back into inventory tone.
- the eighth `Wave 5` report-surface slice is now also live at section-placement level:
  `motor_016.py` now emits `section_surface_inventory_profile` / `section_surface_inventory_state`
  plus `section_inventory_surface_policy_register` / `section_inventory_surface_summary`, and can
  demote registry-like optional sections to appendix when the body spine is already strategically strong.
- the ninth `Wave 5` report-contract slice is now also live:
  `motor_016.py` now treats `required_body_sections` as true hard-protection only, keeps
  `preferred_body_sections` separate from `resolved_body_sections`, and records
  `demoted_preferred_body_sections` when optional outline sections lose the main surface.
- the tenth `Wave 5` contract-spine slice is now also live:
  `render_section_contract.py` now shrinks the hard-required structural-first body spine so
  `Scenario Space`, `Conditional Redesign Pathways`, `What Not To Do Yet`, and
  `Claim Permission Matrix` are no longer mandatory body survivors by contract.

Interpretation:

- the current congruence lane is green and remains protected;
- the bounded runtime-authority phase of the new skill is now closed;
- the new autonomous-research-loop deepening lane is now actively started in code, not only planned;
- the skill has centralized and governed the key runtime reasoning already spread across `motor_049`, `motor_052`, `motor_053`, and `motor_054`;
- no wholesale runtime rewrite was required;
- the remaining open items are optional next-phase depth, not blockers for this closure.
- the autonomous research loop now has a sovereign run-scoped state, job queue, metrics, stop-condition record,
  and event log, all persisted under `run-registry/research-loop-*` and surfaced in `dashboard.congruence_brain`.
- the stronger `research_brain_100_percent_completion` lane is now also started in code:
  search-result intake now accepts structured records / JSON arrays in addition to packet blocks, and the
  runtime now persists `import_format` plus richer batch templates for imported search-result options.
- the same lane now also promotes imported search results through an option-level sovereign queue:
  `current_search_query_result_option_row` now represents the active imported option, not only the candidate,
  and the dashboard can promote that current option directly without a numeric prompt when the runtime already
  knows which imported option is active.
- the non-paper source-family expansion is now operational beyond campaign metadata:
  accepted/manual visible references from `utility`, `OEM`, `regulatory`, and `specialist web` families can
  generate governed `L2` knowledge atoms directly, even before a full extraction record exists.
- the upstream search lane is now less glue-heavy:
  imported search-result options can be converted directly into excerpt-backed references in one step,
  while still preserving the rule that imported snippets are not evidence.
- that compressed lane now also has a batch variant for visible imported-result options:
  multiple candidate-unique source hits can be resolved to excerpt-backed references in one call,
  while still requiring real excerpts and preserving imported snippets as non-evidence.
- the upstream search lane now also auto-captures singleton imports when requested:
  candidates with exactly one visible imported result can skip imported-option review and move directly
  to `result_captured_ready_for_excerpt`, while candidates with multiple options remain in governed review.
- the same upstream lane now supports an `ordered` import format:
  a batch of visible results can be pasted in the active batch-plan order without repeating `candidate_id`,
  and the runtime maps those rows deterministically onto the current candidate queue.
- singleton imports can now also carry `reference_excerpt` directly:
  if the operator already has a real visible excerpt at import time, the framework can auto-resolve that
  singleton straight to `manual_text_enriched` instead of stopping at `result_captured`.
- the ordered import lane no longer requires JSON only:
  the runtime now also accepts batch-ordered packet blocks without `candidate_id`,
  mapping them deterministically from the active batch plan.
- visible-result import can now also carry an explicit chosen-result marker:
  `Selected: yes` in packet lanes and `"selected": true` in structured/JSON lanes let the operator identify the
  correct visible result at import time, so multi-option candidates can skip later imported-option review for that
  candidate without treating the imported snippet as evidence.
- when that explicitly selected visible result already includes a real `reference_excerpt`, the same import call can
  now resolve straight to `manual_text_enriched`; otherwise it stops correctly at governed `source-hit` capture.
- the ordered import lane now also accepts a compact per-line format:
  `URL | Title | Snippet | Excerpt | Selected | Notes`, mapped in active batch order,
  so operators can import one visible result per pending query-seed row with less packet boilerplate.
- the same ordered import lane now also accepts TSV / clipboard rows:
  `URL<TAB>Title<TAB>Snippet<TAB>Excerpt<TAB>Selected<TAB>Notes`, still mapped in active batch order,
  so copy/paste from tables or spreadsheet-like result views requires less manual reshaping.
- that TSV lane now also accepts title-first clipboard order:
  `Title<TAB>URL<TAB>Snippet<TAB>Excerpt<TAB>Selected<TAB>Notes`, so result views that place the label before the
  link no longer need manual column swapping before paste.
- the same compact/TSV lane now also accepts embedded-link first columns:
  `[Title](URL)` or `Title (URL)` can stand in for the URL+Title pair, so copies from notes or markdown-heavy
  result views can still map cleanly into `Snippet / Excerpt / Selected / Notes`.
- TSV clipboard rows can now also include extra leading/intermediate columns before the URL:
  the runtime infers `title / URL / snippet` from the row and folds the remaining cells into `notes`,
  so table-like copies such as `Rank<TAB>Title<TAB>Year<TAB>Source<TAB>URL<TAB>Snippet` no longer need manual reshaping.
- that TSV clipboard lane is now also robust against common header-shaped result tables:
  recognizable result-table columns are preserved as structured import, while non-core cells fall into `notes`
  instead of making the row unusable.
- the same TSV lane now also supports explicit semantic header rows in flexible order:
  commented or plain headers like `Source / Year / Abstract / Link / Title / Selected / Notes`
  are mapped by meaning rather than by fixed column position.
- the ordered import lane now also exposes a provider-aware capture guide in the batch plan:
  provider-specific preferred headers, snippet fallbacks, and note-preserved columns are now surfaced
  before paste, and provider-native fields like `Document Link` / `Index Terms` are now accepted as
  first-class URL / visible-text inputs instead of degrading to manual cleanup.
- the same provider-aware lane now also accepts provider-native TSV rows with no pasted header line,
  as long as the row shape matches one of the provider layouts published in the batch plan; this keeps
  `IEEE` / `Scopus` table copies closer to raw search output instead of forcing manual header reconstruction.
- the same search batch lane now also exposes a commented provider capture sheet in runtime and UI:
  `Current Search Result Capture` can now show a row-by-row worksheet keyed to the active candidates and
  primary queries, reducing the amount of restructuring needed before pasting visible provider rows.
- the same lane now also exposes a provider-native search sheet before capture:
  `Current Search Result Capture` can show search-ready lines, preferred surface, provider tips, and
  result-capture goals per active candidate group, so the operator no longer starts provider search from a blank prompt.
- the same lane now also exposes a combined provider search workbook:
  one artifact can now carry search-ready lines plus import-ready `Candidate ID / URL / Title / Snippet / Selected / Excerpt / Notes`,
  and that workbook is accepted back by the sovereign import parser without a separate reshaping step.
- the same upstream lane now also exposes a live `search_query_execution_session_bundle` in payload:
  `Current Search Result Capture` can now show the session bundle directly and import
  `search_query_execution_session_rows` from structured JSON without requiring search-plan
  materialization first, while still materializing a persisted manifest when the operator requests it.
- the same lane now also has a run-scoped `search session store`:
  operators can `Save session bundle` into `run-registry/search-query-execution-sessions/<run_id>.json`
  and then `Import saved ready rows` directly, so search-result capture work survives refresh/reentry
  without rebuilding the structured payload each time.
- that same lane now also has native in-page visible-result capture:
  `Current Search Result Capture` now renders the live `search session rows` as editable row forms
  (`source_url`, `title`, `snippet`, `excerpt`, `selected`, `notes`), can save those rows to the
  run-scoped store, and can import the ready rows from that UI without making `window.prompt` the
  primary operator path for this lane.
- that same native lane now also supports row-local provider parsing:
  a single pasted provider row can now be parsed directly into the row form and used as the basis
  for save/import, instead of forcing manual field-by-field transcription.
- the strategic-regression closure lane is now started in runtime, not only diagnosed:
  `motor_016` no longer collapses bounded structural intelligence into a thin blocked shell, and now
  surfaces wrong-variable risk, hidden boundary error, invalid comparison risk, dominant loss logic,
  strategic nuggets, and bounded conditional pathways in the client-facing package when local claim
  closure is still blocked.
- the chart lane now follows the same correction:
  `motor_018` uses `executive_thesis` as a governed fallback surface for fair-comparison, cross-layer,
  and cost-driver charts, so the visual layer no longer drops strategic intelligence when the thesis
  is strong but some upstream registers are sparse.
- the final render/delivery lane is now certified too:
  `test_operational_intelligence_skill_cutover.py` now proves that the rendered `.tex` / PDF package
  still carries bounded strategic language like wrong-variable / wrong-denominator / fair-comparison
  logic and claim prohibitions, and that `motor_017` preserves chart assets end-to-end when the
  report package carries governed chart surfaces.
- the strict `RB100-08` review is now also closed in governance:
  `research_brain_100_percent_completion_review_latest.md` explicitly records that the expanded
  research-brain target is still not honestly `100%` certifiable because the rich-source `100+`
  execution path is not yet acceptance-proven and core research transitions still retain too much
  `window.prompt` glue.
- the stronger `research_brain_100_percent_completion` lane now also has its first floor-doctrine hardening slice:
  the target-floor logic emits an explicit `bootstrap_floor_exception`, shallow normal runs no longer silently
  collapse to `20`, and depth/gap records now surface the floor-policy state and reason directly.
- the draft-resolution lane now also has a sovereign helper for prefilled query-seed resolution, optional article
  identity updates (`title`, `doi`, `URL`), and one-step auto-accept for discovery candidates when a real excerpt
  has been captured.
- the atom-refresh and rerank lane now also has sovereign before/after summaries plus persisted run-scoped stores,
  so the framework can explain when enriched references changed atoms, coverage, latent combinations or the review queue.
- the autonomous-research-loop lane now also has sovereign source-depth enforcement:
  `combination_gap_analyzer` no longer uses a flat latent-pool target, the loop now emits a
  `research_depth_enforcement_record`, and the stop gate now blocks premature saturation unless source-family depth,
  high-priority-family coverage, and latent-floor policy are all coherent enough to stop.
- the autonomous-research-loop lane now also has sovereign operator control and auditable stop states:
  `research-loop-controls/<run_id>.json` persists `resume / pause / stop`, the loop now emits
  `paused_by_operator`, `stopped_by_operator`, and `stopped_by_saturation`, and `Congruence Brain`
  now exposes explicit stop reasons from payload instead of implicit UI behavior.
- the dashboard control-room slice is now also started in code:
  `Congruence Brain` can now `Advance loop`, `Pause loop`, `Resume loop`, `Stop loop`,
  `Force rerank`, and `Mark source family exhausted`; those actions now hit sovereign endpoints
  and persist run-scoped control or trigger state instead of remaining UI-only concepts.
- the autonomous-research-loop lane now also has family-level acceptance certification:
  `warehouse`, `manufacturing`, and `building` each have a dedicated executable bundle in
  `test_autonomous_research_loop_acceptance.py`, including the negative guarantees that
  `query_seed_draft` never counts as evidence, under-investigated runs cannot stop, and duplicate
  latent rows cannot fake the `50+` combination floor.
- loop metrics and the combination-gap analyzer now deduplicate latent/admissible combinations by
  `combination_id` before enforcing saturation or search-depth policy, while preserving raw row counts
  for audit so campaign inflation remains visible instead of silently passing.
- the dashboard now also runs `query_seed_draft` review as a sovereign one-by-one queue:
  `Current Reference Draft Review` uses `reference_resolution_sequence_register`,
  `current_reference_resolution_row`, and `next_reference_resolution_rows` so the operator resolves
  one draft and the framework automatically presents the next pending draft on refresh.
- `Resolve draft` is now also packet-based in one step:
  `POST /api/article-reference-resolve-packet` accepts a structured multiline packet
  (`URL`, `Title`, `DOI`, `Journal`, `Year`, `Notes`, `Excerpt`) and resolves the draft without
  the previous chain of browser prompts.
- the draft-resolution lane now also supports `Resolve visible batch`:
  the dashboard can resolve multiple visible `query_seed_draft` rows in one operator step through
  `POST /api/article-reference-resolve-batch`, using packet blocks separated by `---`.
- that batch resolution is now guided by a sovereign plan:
  `reference_resolution_batch_plan` prefers drafts from the same provider first, then the same
  `source_family`, so the operator is not nudged into mixing heterogeneous source contexts in one packet.
- the current cutover certification chain now survives `motor_047 -> motor_048 -> motor_016 -> motor_017 -> motor_027`
  without synthetic `motor_014`, `motor_015`, `motor_034`, or `motor_037..046` injections inside the end-to-end warehouse / manufacturing / building certification helper.
- `motor_016` now also synthesizes bounded `output_blocks`, `report_readiness_register`, `claim_permission_register`,
  and `decision_permission_register` from skill-first surfaces when `motor_014` / `motor_015` are absent, so those
  motors are no longer hard dependencies of the package chain.
- `motor_016` also synthesizes bounded `inference_records`, `conflict_register`, `validation_queue`,
  `next_best_questions`, `evidence_gap_register`, and `opportunity_candidates` from skill-first surfaces when
  `motor_014` is absent, so visible validation-question output no longer collapses in the cutover path.
- a second cutover certification layer now exists using a generic `live-bundle` runtime wrapper built only from
  the real family case bundle plus bounded runtime metadata, proving the `warehouse`, `manufacturing`, and
  `building` chains no longer depend on the family-specific synthetic helper to preserve authority state or
  visible validation-question output.
- a third cutover certification layer now exists using a `routing-ready live-bundle` wrapper that runs
  `Motor035Adapter` for real and feeds its routing/jurisdiction output into the same downstream chain, proving the
  authority split and visible validation agenda survive even when the upstream wrapper is reduced further.
- the remaining synthetic `__runtime__` and `compliance_applicability_case` assembly used by the cutover tests has
  now been moved into `runtime_orchestrator.zlab_skill.runtime_bridge.build_skill_first_runtime_wrapper_inputs`,
  so the certification helpers consume a bounded runtime utility from the framework instead of hand-building that
  wrapper inside the test file.
- `patterns` is now also promoted to `skill_primary` when the registry-first activation surface fully covers the
  active mapped runtime pattern set.
- `extraction_review` now closes into approved promotion registers: reviewed pattern and combination candidates can
  now materialize as validated sovereign spec proposals for registry review.
- the licensed lane now has an executable end-to-end ingestion path from acquisition plan/result to extraction,
  review and promotion registers, even when provider-authenticated live acquisition remains credential-gated.
- the dashboard `/api/live` and `congruence_brain` surface now expose pattern authority, authoritative pattern
  activation, licensed-lane provider session state, and extraction-review / promotion summaries so the new closure
  is operationally visible before real provider credentials are loaded.
- the dashboard now also persists adjudication decisions for extraction-derived pattern/combination promotions,
  so registry-review acceptance can be tracked per run before live provider sessions are populated.
- accepted promotion decisions now materialize a run-level `registry_review_bundle`, exposed in dashboard payloads
  and through an export endpoint, so registry-review handoff is ready before live provider sessions are populated.
- accepted promotion decisions now also materialize a `registry_stage_preview`, showing the exact candidate file plan
  (`patterns/*.json` / `combinations/*.json`) that would be staged into the sovereign registry before any write step.
- the dashboard can now materialize those candidate files into a run-scoped local staging area outside the sovereign
  registry and persist a manifest, so registry review can inspect real JSON artifacts before merge.
- the dashboard can now also materialize a run-scoped provider-session handoff manifest with per-provider
  recommended actions, profile paths and sample entry URLs, so live licensed-session setup no longer needs ad hoc prep.
- that provider-session handoff now also includes executable bootstrap and auth-validation commands per provider,
  so the live login step is no longer implicit or ad hoc once credentials are available.
- the licensed lane now also supports `institution-first` access routing, where a shared university login profile
  can be bootstrapped once and then validated against `Scopus`, `Elsevier`, `IEEE`, and `Springer` as downstream providers.
- `Scopus` has now been validated successfully through the institutional proxy path, and the lane also has a governed
  fallback for provider-hardblocked full text: `local licensed PDF -> extraction_review -> promotion_review`.
- that local licensed artifact fallback now also has a template scaffolder, so a folder of PDFs can be turned into
  `.metadata.json` and `.extraction.json` sidecars in one step before governed ingestion.
- that same local-PDF lane can now auto-generate a governed `auto_draft` extraction from PDF text plus metadata when
  `.extraction.json` is missing, derive registry-mapped pattern/combination candidates, and persist the draft sidecar
  next to the PDF so the operator only needs to accept, reject, or modify the resulting review surfaces.
- a new `Scopus discovery export -> candidate PDF queue` lane now exists, so a `.csv`/`.json` export can be converted
  into ranked candidate documents, expected PDF filenames, inbox metadata/extraction sidecars, and reviewable
  pattern/combination drafts before full text is even downloaded.
- that discovery-export queue is now provider-generic for `Scopus`, `IEEE`, `Elsevier`, and `Springer`, while keeping
  the old `Scopus` entrypoint as a compatibility alias.
- the dashboard now understands that discovery queue directly: candidates can be imported, accepted/rejected,
  modified, and reference-read from the UI, while article references are persisted per run without downloading PDFs.
- the dashboard now also supports batch reference reading for discovery candidates already accepted for follow-up,
  and it materializes a run-scoped `accepted_discovery_candidate_bundle` so approved literature candidates can be
  handed off downstream as a persistent bundle instead of isolated per-row decisions.
- certification now also exists from raw family inputs through `Motor035Adapter`, `motor_049..054`, and the
  downstream `motor_047 -> motor_048 -> motor_016 -> motor_017 -> motor_027` chain.
- `motor_014` / `motor_015` optionality is now explicit in runtime outputs via bounded enrichment state flags,
  not only inferred from survival of the package chain.
- the sovereign pattern activation surface now emits the prompt-literal activation ladder fields
  (`activation_state`, `why_activated`, `what_would_falsify`, `minimum_evidence_to_confirm`) instead of a flat
  placeholder activation state.
- `building` now also promotes to `skill_primary` in the same cutover family as `warehouse` and `manufacturing`.
- the manufacturing lane now has prompt-literal sovereign pattern coverage for `process_load_vs_waste`,
  `maintenance_hidden_value_driver`, and `procurement_vs_lifecycle_cost`, plus a dedicated acceptance bundle.
- `motor_024` now runs a registry-backed `ReportDiversityValidator` over real `report_output` state and blocks
  template-contaminated outputs through the same preflight contract.
- prompt section 20 now has explicit structure-equivalence files under `runtime-orchestrator/zlab_skill/*.yaml`,
  mapping the requested conceptual files to the sovereign registry/runtime implementation actually in use.
- the dashboard can now merge validated staged candidate files into the sovereign registry root with schema validation,
  so the operational loop now reaches `promotion review -> stage -> registry merge` instead of stopping at dry-run staging.
- the dashboard can now also rebuild `auto_draft` extraction and promotion bundles directly from accepted discovery
  references plus visible reference text, so the loop `accepted refs -> reference-backed promotions -> review -> registry`
  is operational before PDFs arrive.
- those reference-backed promotions now also flow into the normal `promotion_review -> registry_review_bundle -> stage -> merge`
  circuit instead of remaining a side manifest.
- promotion rows can now also be modified directly from the dashboard before stage/merge, with run-scoped edit
  persistence and downstream propagation into `promotion_review`, `registry_review_bundle`, staged candidate files,
  and registry merge payloads.
- saved article references can now also be modified directly from the dashboard, including manual excerpt/visible-text
  curation; `manual_text_enriched` references count as enriched evidence for accepted-reference bundles and feed the
  `reference-backed promotions -> review -> registry` loop without requiring a PDF or a perfect automated read.
- the dashboard can now also create a manual licensed article candidate end-to-end with `title + URL + abstract +
  keywords + excerpt`, auto-build the sovereign discovery candidate row, optionally accept it for reference use,
  persist a `manual_text_enriched` article reference, and immediately feed the same `accepted refs -> promotions`
  loop without any export file or PDF.
- accepted references with enriched visible text now auto-refresh the `reference-backed promotions` manifest on
  create/read/edit and batch-read flows, so the explicit dashboard refresh button is no longer required for the
  normal happy path and remains only as a deliberate re-run control.
- a new optional deepening plan now exists in
  `latent_combination_research_expansion_plan_latest.md`, defining how the framework should evolve from a compact
  registered-combination lane into a broader multi-source latent-combination research system that routinely
  surfaces large candidate pools for human adjudication.
- the first runtime slice of that latent-combination expansion is now open in code:
  the framework can build an `asset_context_vector`, derive context differentiators,
  emit a `skill_latent_combination_candidate_register`, and expose a filtered
  `skill_admissible_combination_review_register` without weakening the existing
  registry-first combination lane.
- that slice now also includes `latent_combination_cluster_register` plus dashboard rendering and adjudication support:
  latent/admissible combinations are visible in `Congruence Brain`, grouped into first-pass clusters,
  and can be accepted/rejected through the same run-scoped combination decision store already used by
  registry-exact combinations.
- latent-combination scoring is no longer only `layer span + context presence`; it now also includes
  active-pattern evidence support, `source_basis` richness, and asset-context alignment signals
  such as tariff, boundary, operating-rhythm, solar, and service-intensity relevance.
- latent clusters are now operational UI objects, not only payload artifacts:
  the dashboard can batch-adjudicate admissible latent combinations by cluster and explicitly
  suppress generic-template clusters through the same run-scoped combination decision store.
- latent clusters now also support real run-scoped `split` and `merge` overrides through a dedicated
  `latent-cluster-overrides` store; cluster recomputation happens inside `dashboard._congruence_brain_activity`
  without mutating the base latent candidate generator.
- the dashboard now renders override metadata (`override_state`, `source_cluster_ids`, optional
  `cluster_label`) and exposes `Split cluster` / `Merge cluster` actions directly in `Congruence Brain`.
- the lane now also builds a sovereign `knowledge_atom_register` plus `source_coverage_summary`
  from licensed extraction records, discovery candidates and reference-backed extraction manifests.
- latent-combination synthesis can now expand its `pattern pool` from research-supported atoms even when
  the extra pattern is not already active in runtime, which makes the pool less dependent on the narrow
  active-pattern surface.
- latent candidates now expose `supporting_atom_ids`, `supporting_document_refs`,
  `supporting_provider_keys`, `knowledge_atom_count`, and the score now includes
  `knowledge_atom_support_score` plus `source_diversity_score`.
- the dashboard now renders `Research Coverage` and per-candidate atom/document support inside
  `Congruence Brain`.
- the lane now also exposes a sovereign `combination_search_gap_record` that decides whether
  the current latent pool is still shallow because the framework under-investigated or whether
  coverage is already strong enough to review a thin pool cautiously.
- `Congruence Brain` now renders `Combination Search Gap` with `search_status`, `severity`,
  `gap_flags`, and `recommended_actions`.
- the lane now also formalizes a sovereign `research_campaign_record` plus
  `source_family_coverage_register`, so the dashboard can show campaign status,
  touched vs missing source families, and next source-expansion actions.
- `Congruence Brain` now renders `Research Campaign` and one card per source family
  with coverage state, provider touch, document counts, and knowledge-atom depth.
- current full-suite truth after the research-campaign slice is now `619 passed, 15 warnings`.

## Objective

Create the sovereign knowledge layer for:

- structural patterns;
- falsifiable hypotheses;
- variable relations;
- minimum evidence;
- claim boundaries;
- fair-comparison gates;
- financial exposure translations;
- TAD actions;
- pattern combinations;
- run-level combination adjudication.

This lane becomes the structured operational-financial brain of the framework.

## Absolute rules

Never weaken:

- no pattern as diagnosis;
- no ROI without evidence;
- no savings without baseline + measure + control;
- no benchmark as local truth;
- no fair comparison without normalization;
- no digital twin before dominant variables;
- no sensors before hypothesis materiality;
- no peer superiority without evidence;
- no claim without evidence state;
- no memory transfer without scope control;
- no LLM authority over truth;
- no report generation if validators fail.

## Do-not-break

Do not destabilize:

- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_049.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_052.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_053.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_054.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_016.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_017.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_024.py`
- artifact consistency blocking
- claim consistency blocking
- declared-input downgrade
- fair-comparison invalidity enforcement
- browser-lane policy and provenance

Do not reintroduce:

- string-only blocked-claim governance;
- source contamination;
- stale chart leakage;
- uncontrolled browser crawling;
- benchmark-as-local-truth;
- static-intake final logic;
- question-absence boundedness.

## Delivery shape

The new lane will be split into:

1. `runtime-orchestrator/zlab_skill/registry/`
   versioned pattern, combination, source-basis and validator documents

2. `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/`
   schema, loader, activation, combination, validation, adjudication and memory code

3. existing congruence motors as thin runtime consumers

4. dashboard adjudication surfaces for combination acceptance / rejection per run

5. licensed research ingestion lane using `Playwright` plus scoped provider sessions

## Phase order

1. `OISK-00` baseline freeze
2. `OISK-01` sovereign registry and schema foundation
3. `OISK-02` licensed research acquisition lane
4. `OISK-03` structured extraction lane
5. `OISK-04` twenty foundational patterns
6. `OISK-05` combination engine
7. `OISK-06` validator engine
8. `OISK-07` dashboard adjudication
9. `OISK-08` memory system
10. `OISK-09` motor integration and migration
11. `OISK-10` acceptance, cutover and certification

---

## `OISK-00` Baseline Freeze

Purpose:

- freeze the current congruence lane before registry-first refactor

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/tests/test_warehouse_dynamic_congruence_acceptance.py`
- `runtime-orchestrator/tests/test_dynamic_congruence_baseline_contracts.py`
- `runtime-orchestrator/tests/fixtures/dynamic_congruence_register_contract_snapshot.json`

Required outputs:

- fresh snapshots for `motor_049`, `motor_052`, `motor_053`, `motor_054`
- frozen dashboard payload contract for `/api/live`
- baseline cases for `warehouse`, `manufacturing`, `building`

Acceptance:

- no-regression baseline exists for current lane
- current outputs are snapshotted before registry migration

Current progress:

- skill-registry seed contract exists
- `/api/live` top-level contract snapshot now exists for controlled dashboard expansion
- `congruence_brain` payload branch is now explicitly under contract

Tests:

- directed baseline bundle
- full `pytest -q`

---

## `OISK-01` Sovereign Registry And Schema Foundation

Purpose:

- establish a governed, versioned skill registry outside hardcoded pattern logic

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/__init__.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/schema.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/loader.py`
- `runtime-orchestrator/zlab_skill/registry/README.md`
- `runtime-orchestrator/zlab_skill/registry/patterns/*.json`
- `runtime-orchestrator/zlab_skill/registry/combinations/*.json`
- `runtime-orchestrator/zlab_skill/registry/source_basis/*.json`
- `runtime-orchestrator/tests/test_zlab_skill_registry.py`

Outputs to add:

- `PatternSpec`
- `CombinationSpec`
- `SourceBasisRecord`
- `RegistryBundle`

Acceptance:

- skill registry loads from disk
- all seed docs validate
- pattern refs and source-basis refs cross-validate
- no runtime motor behavior changes yet

Tests:

- `tests/test_zlab_skill_registry.py`
- full `pytest -q`

Current progress:

- base package scaffolded
- initial schema validation scaffolded
- initial pattern / combination / source-basis seeds scaffolded

---

## `OISK-02` Licensed Research Acquisition Lane

Purpose:

- acquire licensed and public technical literature for skill growth without polluting case-evidence truth

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/licensed_acquisition.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/provider_sessions.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/playwright_profiles.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/research_manifest.py`
- `runtime-orchestrator/tests/test_zlab_skill_licensed_acquisition.py`

Source families:

- `scopus_discovery`
- `elsevier_fulltext`
- `ieee_fulltext`
- `springer_fulltext`
- `ashrae_public_guidance`
- `doe_epa_public_guidance`

Rules:

- provider sessions stay outside Git
- raw licensed PDFs stay outside Git
- only metadata, hashes, extracts and provenance enter the repo
- `Playwright` is bounded, session-backed and domain allowlisted

Acceptance:

- provider session routing works
- source provenance manifest is emitted
- extraction lane receives licensed-document manifests, not silent browser blobs

Current progress:

- provider session routing scaffolded
- persistent-profile planning scaffolded
- research-document manifest scaffolded
- acquisition-plan tests scaffolded
- persistent `Playwright` session fetch scaffolded with login-gate and redirect-boundary detection
- executable acquisition wrapper now returns acquisition plan + result + research-document manifest
- acquisition now hands off `extraction_seed` so blocked or successful documents both enter governed extraction review

---

## `OISK-03` Structured Extraction Lane

Purpose:

- transform literature into governed knowledge atoms rather than free text

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/extraction_schema.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/extractor.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/extraction_review.py`
- `runtime-orchestrator/tests/test_zlab_skill_extraction.py`

Outputs to add:

- `KnowledgeExtractionRecord`
- `ExtractionProvenanceRecord`
- `PatternCandidateRecord`
- `CombinationCandidateRecord`

Mandatory extracted fields:

- asset archetype
- process logic
- loss pattern
- anti-triggers
- falsification conditions
- minimum evidence
- financial mechanism
- fair-comparison rule
- measurement minimality rule
- source basis
- confidence ceiling

Acceptance:

- extraction records are versioned
- source provenance is mandatory
- no direct local diagnosis is emitted from literature extraction

Current progress:

- `KnowledgeExtractionRecord`, `PatternCandidateRecord` and `CombinationCandidateRecord` validation now exist
- extraction drafts now derive directly from `research_document_manifest`
- extraction review register now exists for governed human review
- extraction tests now lock the `L2` ceiling and traceability boundary

---

## `OISK-04` Twenty Foundational Patterns

Purpose:

- encode the first governed pattern corpus required by the prompt

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/zlab_skill/registry/patterns/*.json`
- `runtime-orchestrator/tests/test_zlab_skill_pattern_activation.py`

Required first set:

1. warehouse MHE charging
2. dock infiltration
3. cold-chain classification unknown
4. control boundary leakage
5. reactive power exposure
6. HVAC schedule drift
7. high-bay lighting
8. compressed air leaks
9. steam traps
10. boiler degradation
11. chiller degradation
12. maintenance maturity not evidenced
13. demand charge exposure unknown
14. fair comparison invalidity
15. digital twin prematurity
16. sensor prematurity
17. tenant/operator boundary
18. benchmark denominator error
19. procurement vs maintenance conflict
20. compliance vs control mismatch

Acceptance:

- each pattern matches sovereign schema
- each pattern has falsification and minimum evidence
- each pattern has claim-language constraints
- each pattern caps at `L2` without case evidence

Current progress:

- first seed pack now includes warehouse tariff, dock, boundary, fair-comparison, cold-chain, reactive-power and prematurity patterns
- registry now also includes high-bay lighting, compressed-air leak plausibility, maintenance-maturity, demand-charge and compliance/control-mismatch patterns
- the full 20-pattern foundational pack is now present in registry with pattern-pack tests

---

## `OISK-05` Combination Engine

Purpose:

- elevate multi-pattern reasoning into governed combination objects

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/combination_engine.py`
- `runtime-orchestrator/zlab_skill/registry/combinations/*.json`
- `runtime-orchestrator/tests/test_zlab_skill_combinations.py`

Outputs to add:

- `CombinationActivationRecord`
- `CombinationUseConstraintRecord`

Each activated combination must emit:

- `combined_hypothesis`
- `strategic_risk`
- `minimum_evidence`
- `financial_exposure`
- `tad_action`
- `prohibited_claims`
- `what_falsifies`
- `evidence_state_ceiling`
- `source_basis`

Acceptance:

- combinations are explicit objects
- no gold nugget or TAD path can bypass combination governance when combination logic exists

Current progress:

- sovereign combination activation scaffolded
- review-register scaffolded and now consumable by dashboard adjudication
- seed warehouse combo now activates as an auditable candidate object
- manufacturing support-utility combo now exists as a second governed combination path

---

## `OISK-06` Validator Engine

Purpose:

- block misuse of patterns, combinations and financial language

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/validator_engine.py`
- `runtime-orchestrator/zlab_skill/registry/validators/*.json`
- `runtime-orchestrator/tests/test_zlab_skill_validators.py`

Validators to implement:

- `ClaimGovernorValidator`
- `PatternToClaimValidator`
- `FairComparisonValidator`
- `FinancialOutputValidator`
- `TADConsistencyValidator`
- `MemoryScopeValidator`
- `ReportDiversityValidator`
- `SourceTraceabilityValidator`
- `CombinationUseValidator`

Acceptance:

- validator failure blocks hardening of language and report completion when required
- pattern cannot silently become diagnosis
- ROI / IRR / payback remain blocked without evidence basis

Current progress:

- first registry-first combination validator layer now exists
- `registry/validators/` now exists with the first sovereign validator catalog
- validator engine now consumes registry specs instead of relying only on hardcoded fallback logic
- blocked combinations now surface `blocked_by_validator` in dashboard adjudication
- forbidden financial language is now blocked at combination-contract level without punishing `PROHIBIT ROI` guardrails

---

## `OISK-07` Dashboard Adjudication

Purpose:

- let the operator accept or reject each pattern combination for run-level strategic use

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/dashboard.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/adjudication_engine.py`
- `runtime-orchestrator/tests/test_operational_intelligence_dashboard_congruence_brain.py`

Outputs to add:

- `combination_review_register`
- `combination_decision_register`

Decision states:

- `candidate`
- `blocked_by_validator`
- `accepted_for_case_use`
- `rejected_for_case_use`
- `needs_review`

Important rule:

- accepting a combination admits bounded strategic use for that run;
- it does **not** upgrade evidence or convert the combination into local truth.

Acceptance:

- `/api/live` exposes combination review data
- dashboard renders accept / reject state cleanly
- decision provenance and reason are stored

Current progress:

- `congruence_brain` now ships in `/api/live`
- combination decisions now persist per `run_id`
- the dashboard now renders `Accept / Needs review / Reject` controls for active combinations
- adjudication state now round-trips through API + persistence + live payload
- validator-blocked combinations are now prevented from being accepted through the dashboard

---

## `OISK-08` Memory System

Purpose:

- keep approved knowledge and previous validations without leaking truth across scope boundaries

Priority:

- `P1`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/memory_engine.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/memory_scope.py`
- `runtime-orchestrator/tests/test_zlab_skill_memory_scope.py`

Memory classes:

- `pattern_memory`
- `validation_memory`
- `company_memory`
- `source_memory`
- `contradiction_memory`

Acceptance:

- each record is versioned
- each record is reversible
- each record carries scope and reason
- cross-company transfer is blocked unless explicitly allowed

Current progress:

- `registry/memory_policies/` now exists with sovereign policies for `pattern_memory`, `validation_memory`, `company_memory`, `source_memory` and `contradiction_memory`
- `memory_scope.py` now validates record shape, policy alignment, scope discipline and non-promotion of truth
- `memory_engine.py` now emits an admissibility register plus summary counts for priority/context/routing use modes
- cross-company, provider-family and reversal semantics are now executable and covered by tests
- memory now remains explicitly bounded to priority/context/routing support and never upgrades local case truth

---

## `OISK-09` Motor Integration And Migration

Purpose:

- make existing motors consume the new skill instead of owning fragmented pattern logic

Priority:

- `P0`

Status:

- `completed`

Main files:

- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_049.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_052.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_053.py`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_054.py`

Migration direction:

- `motor_049` continues to emit case context, blockers and dominant hypotheses
- `motor_052` becomes registry-first for patterns
- `motor_053` becomes registry-first for financial translation
- `motor_054` becomes registry-first for combinations, TAD, nuggets and claim contracts

Acceptance:

- old hardcoded logic is either retired or reduced to compatibility fallback
- registry-first outputs remain additive before full cutover

Current progress:

- `runtime_bridge.py` now centralizes runtime-to-registry pattern mapping shared by `dashboard.py` and motor consumers
- `motor_052` now emits `skill_active_pattern_ids`, `skill_active_pattern_sources` and `skill_pattern_activation_register` as additive registry-first outputs
- `motor_053` now emits `skill_financial_exposure_register` with governed exposure categories and `L2` ceiling discipline
- `motor_054` now emits `skill_combination_activation_register`, `skill_combination_review_register`, `skill_expanded_tad_action_register` and `skill_gold_nugget_register`
- `motor_053` now also emits `skill_financial_exposure_summary`
- `motor_054` now emits `skill_cutover_authority_register` so cutover state is explicit by domain (`patterns`, `financial_exposure`, `combinations`, `tad`, `gold_nuggets`)
- the dashboard now exposes `registry_pattern_activation_register` from the same shared bridge, reducing mapping drift between live monitoring and runtime adapters
- the initial migration remains additive: legacy heuristic outputs are still preserved while registry-first parity is measured
- shared anti-trigger logic now avoids suppressing invalid-comparison combinations just because a separate comparable row exists elsewhere in the same run
- cutover-oriented tests now exist and lock shadow/authority state before any domain is promoted to `skill_primary`
- `financial_exposure` is now the first promoted domain: `motor_053.authoritative_financial_exposure_register` is `skill_primary`, and `motor_054` consumes it as the authoritative financial lane
- `tad` is now the second promoted domain: `motor_054.authoritative_tad_action_register` is `skill_primary` when the required governed action surface is present
- `gold_nuggets` is now the third promoted domain: `motor_054.authoritative_gold_nugget_register` is `skill_primary` when the skill lane produces `3-5` bounded nuggets covering comparison invalidity, tariff orchestration and boundary leakage
- `congruence_claim_contract_register` now reads the authoritative nugget surface and records `motor_054.authoritative_gold_nugget_register` as supporting source when the promotion gate is met
- `prohibited_action_register` now follows authoritative TAD rather than the legacy TAD register when the promoted surface is complete
- `motor_047` now prefers `motor_054.authoritative_gold_nugget_register` when `gold_nugget_authority_state` is `skill_primary`, and `executive_thesis` now exposes `gold_nugget_source_register` plus `gold_nugget_authority_state`
- `motor_036` now also prefers `motor_054.authoritative_gold_nugget_register` when the gold-nugget lane is promoted, so the system-consistency gate no longer depends on the legacy nugget alias
- report compression now traces `executive_structural_thesis` against `motor_054.authoritative_gold_nugget_register | motor_054.strategic_gold_nugget_register`, so downstream thesis provenance is explicit during cutover
- `motor_048.main_report_outline` now preserves `gold_nugget_authority_state` and `gold_nugget_source_register`, so report-outline/package surfaces keep the same bounded authority semantics exposed by `executive_thesis`
- `motor_016` now carries `gold_nugget_authority_state` and `gold_nugget_source_register` into `structural_executive_summary` and `structural_intelligence_summary`, so package consumers do not have to infer nugget authority only from nested thesis/outline structures
- `motor_017` now emits `gold_nugget_authority_state` and `gold_nugget_source_register` directly in the render output, so render consumers can read authority without reopening the package
- `motor_027.delivery_manifest` now exposes `gold_nugget_authority_state` and `gold_nugget_source_register` both top-level and inside `governance_summary`, so delivery/export consumers preserve the same authority semantics as package/runtime
- skill gold-nugget synthesis is now asset-sensitive: `building` no longer inherits warehouse tariff/logistics nuggets, and `manufacturing` no longer inherits warehouse charging nuggets
- multi-case cutover truth is now explicit in tests: `warehouse` and `manufacturing` promote `gold_nuggets` to `skill_primary`, while `building` remains bounded in `legacy_primary_skill_shadow`
- report/package cutover coverage is now explicit in tests: `building` preserves its bounded shadow state through `Motor048`, while a manufacturing-like outline preserves `skill_primary` authority and authoritative nugget provenance
- package-summary cutover coverage is now explicit in tests: `motor_016` summaries inherit the same authority/source semantics as `main_report_outline` or, when that outline is absent, fall back to `executive_thesis`
- render/delivery cutover coverage is now explicit in tests: `motor_017` preserves both shadow and `skill_primary` authority states, and `motor_027` keeps the same authority/source in the final delivery manifest
- multi-case render/delivery certification is now explicit across `warehouse`, `manufacturing` and `building`: family-specific authority states survive `motor_017 -> motor_027` end-to-end when seeded from real runtime `motor_054` outputs and governed package contracts
- mixed-runtime package certification is now explicit across `warehouse`, `manufacturing` and `building`: family-specific authority states survive `motor_047 -> motor_048 -> motor_016 -> motor_017 -> motor_027` when driven by real family runtime signals from `motor_049..054` plus the minimum structural bridge still required upstream of `motor_047`
- `motor_047` now reduces that bridge directly: when `motor_033/034/037..046` are missing, it can synthesize a bounded structural-thesis context from real skill-first runtime surfaces (`motor_051/053/054`) plus target metadata, preserving `skill_primary` for `warehouse` / `manufacturing` and `legacy_primary_skill_shadow` for `building`
- direct cutover tests now prove that `motor_047` can build an admissible executive thesis for `warehouse`, `manufacturing` and `building` using only minimal family inputs plus live skill-first runtime registers
- `motor_048` now also reduces the bridge directly: when `motor_034` is missing, it can synthesize a bounded report-compression context from `motor_047.executive_thesis` plus `motor_054.congruence_claim_contract_register`, preserving the same authority split by family without reopening the legacy compression path
- direct cutover tests now prove that `motor_048` can build skill-first outlines for `warehouse`, `manufacturing` and `building` using only minimal family thesis inputs plus live skill-first runtime registers
- the bounded wrapper context used by cutover certification is now derived by
  `runtime_orchestrator.zlab_skill.runtime_bridge.build_skill_first_runtime_wrapper_inputs`, so
  the cutover helpers no longer hand-build `__runtime__` or `compliance_applicability_case`

---

## `OISK-10` Acceptance, Cutover And Certification

Purpose:

- prove the super skill is real, bounded and non-hallucinatory before it becomes canonical

Priority:

- `P0`

Status:

- `completed`

Acceptance bundles:

- `warehouse`
- `manufacturing`
- `building`
- `cold_chain`
- `leased owner/operator asset`

Mandatory behavior:

- no pattern as diagnosis
- no ROI language
- no savings language without baseline + measure + control
- no peer superiority without fair comparison
- no sensors before hypothesis discrimination
- no digital twin before dominant variables
- no memory leakage across company scope
- no report generation if combination validators fail

Cutover condition:

- registry-first lane green
- adjudication lane green
- validator lane green
- warehouse/manufacturing/building acceptance bundles green
- full `pytest -q` green

Current progress:

- `warehouse`, `manufacturing`, and `building` cutover certification is green through package, render and delivery
- the bounded wrapper used by those certifications is now framework-owned rather than test-owned
- current suite truth for closure is `566 passed, 15 warnings`

## Phase closure boundary

This bounded phase is now closed.

What is closed:

1. registry-first runtime authority for `financial_exposure`, `tad`, and `gold_nuggets`
2. dashboard adjudication with validator-aware combination acceptance / rejection
3. memory scope enforcement with bounded non-truth promotion rules
4. multi-case cutover certification for `warehouse`, `manufacturing`, and `building`
5. package, render, and delivery propagation of authority/source semantics
6. runtime-owned wrapper synthesis for the bounded `__runtime__` and `compliance_applicability_case` surfaces used by cutover certification

What is intentionally deferred and non-blocking:

1. provider-authenticated literature population at scale
2. extraction-review to approved-pattern promotion workflow
3. certification further upstream than the current `Motor035Adapter`-backed bounded wrapper
4. deeper replacement of optional `motor_014` enrichment surfaces beyond what the cutover chain needs

Interpretation:

- there is no mandatory next slice inside this phase;
- any further work starts a new optional deepening phase, not a continuation of unresolved closure debt.
- the most strategically important optional next phase is now the latent-combination research expansion documented in
  `latent_combination_research_expansion_plan_latest.md`.

## Latest optional deepening progress

- `latent_combination_candidate_register` is now asset-context-specific rather than template-like and is visible/adjudicable in `Congruence Brain`.
- the lane now expands latent-combination synthesis with `knowledge_atom_register`, `source_coverage_summary`,
  `combination_search_gap_record`, and `research_campaign_record`.
- each `source_family` now has a sovereign `deeper_search` trigger plan plus persisted queue state in
  `run-registry/research-campaign-triggers/<run_id>.json`.
- the dashboard now exposes `Trigger deeper search` per source family through `POST /api/source-family-trigger`.
- combinations can now be reviewed sequentially through `current_combination_review_row` and
  modified via `POST /api/combination-edit`, which persists run-scoped edits and advances the queue.
- the sequential queue now also supports run-scoped `defer` and `batch size` controls through
  `POST /api/combination-review-control`.
- combination review outcomes can now auto-queue follow-on research through
  `combination_follow_on_research_register` plus persisted `research-campaign-triggers/<run_id>.json`.
- each active combination can now also produce and materialize a concrete execution plan through
  `combination_follow_on_execution_manifest_register` and
  `combination-follow-on-manifests/<run_id>.json`.
- those execution manifests now also include provider-specific search templates by `query_family`, including
  `search_surface`, `primary_query`, `pivot_query`, `asset_focus_terms`, and `evidence_targets` for providers such as
  `scopus`, `ieee`, `springer`, `elsevier`, `ashrae`, `doe`, and `epa`.
- `Current Combination Review` now renders those provider-level query templates directly, so the operator can move from
  combination adjudication to a concrete research query plan without leaving the dashboard.
- those provider query templates can now also be seeded directly into the existing `Discovery Candidate` lane through
  `POST /api/combination-follow-on-seed-candidates`, creating deterministic `queryseed-*` candidates with provider launch
  URLs, search intent, query notes, and evidence targets.
- the `Current Combination Review` card now exposes `Seed research leads`, which pushes the active combination into the
  same downstream flow already used by `discovery candidates -> article references -> accepted refs -> promotions`.
- `queryseed-*` candidates can now also be converted through `Read reference` into epistemically safe
  `query_seed_draft` article references, which carry the search brief and provider query plan without pretending that a
  real article excerpt has already been read.
- the `Discovery Candidate` card now labels this path as `Draft ref` for `queryseed-*` rows, so the operator can move
  from latent combination to provider-specific search brief with one more click and then replace that draft with a real
  excerpt later.
- `Article Reference` now exposes `Resolve draft` for `query_seed_draft`, letting the operator capture a real article
  URL plus a real visible excerpt in one guided step and promote the draft to `manual_text_enriched` without faking any
  automated reading.
- the edit path now persists `source_url` on the reference record and preserves provenance with
  `acquisition_result.status = query_seed_manual_capture` when a draft is resolved manually.
- `Current Reference Draft Review` now also batch-groups drafts by research intent, not just provider/source:
  it prefers same `query_family` and same evidence targets before falling back to looser grouping.
- `reference_resolution_batch_plan` now exposes `query_families`, `evidence_targets`, and `batch_reason`, so the
  operator can see why a batch is coherent before resolving it.
- `query_seed_draft` resolution now also preserves provider-aware search hints in sovereign prefill:
  `launch_url`, `search_surface`, `execution_hint`, `primary_query`, and `pivot_query`.
- the packet templates used by `Resolve draft` and `Resolve source batch` now include those hints as comments before
  the editable fields, reducing manual reconstruction between query seed and real excerpt capture.
- `query_seed_draft` now also supports `quick resolve` through `POST /api/article-reference-quick-resolve`, so the
  operator can close a draft with just real URL + visible excerpt + notes while still reusing the same sovereign
  edit/rerank path underneath.
- the dashboard UI now makes that shorter path primary as `Resolve excerpt`, while preserving `Resolve full packet`
  for richer metadata correction when needed.
- batch resolution now mirrors the same split:
  - `Resolve source batch` uses a short template with only `Candidate ID + URL + Notes + Excerpt`
  - `Resolve full source batch` preserves the richer packet with `Title/DOI/Journal/Year`
- this reduces copy for the normal path without creating a second batch ingestion lane.
- the dashboard now also exposes `Capture result` for `query_seed_draft`:
  it stores real article URL plus visible result-title/snippet while keeping the record in `query_seed_draft`.
- that gives the next `Resolve excerpt` step better defaults and context without falsely upgrading result text into
  evidence or `manual_text_enriched`.
- the autonomous-research-loop lane now also externalizes two surfaces that were still too dashboard-centric:
  `provider_query_seed_materializer.py` now owns deterministic `queryseed-*` construction from
  follow-on provider query templates, and `research_query_runner.py` now owns the run-visible
  `search_result_capture_register` / `search_result_capture_sequence`.
- `Congruence Brain` now exposes that intermediate lane directly as:
  `search_result_capture_register`, `current_search_result_capture_row`,
  `next_search_result_capture_rows`, and `search_result_capture_summary`, plus a visible
  `Current Search Result Capture` control-room card before excerpt resolution.
- the same lane now also exposes `search_query_execution_register`,
  `current_search_query_execution_row`, `next_search_query_execution_rows`, and
  `search_query_execution_summary`, so each pending `queryseed-*` carries a concrete provider
  search packet, query variants, and a capture packet template before any result is claimed.
- that search-execution lane now also persists a run-scoped manifest through
  `search-query-execution-manifests/<run_id>.json`, and the dashboard can batch-capture multiple
  source hits through `POST /api/article-reference-capture-search-result-batch` before excerpt enrichment.
- the same reference-resolution lane now reuses captured search-result context when resolving
  drafts:
  missing `source_url`/title/notes can be hydrated from the stored capture state, so excerpt
  resolution no longer has to restate an article URL that was already captured.
- `reference_resolution_batch_plan` now emits `captured_ready` plus
  `captured_quick_packet_template`, allowing compatible drafts to resolve in batch with
  `Candidate ID + Excerpt` while reusing the previously captured URLs.
- the same search-execution lane now supports a run-scoped imported-results store through
  `search-query-result-imports/<run_id>.json`.
- pending `queryseed-*` rows now surface `imported_result_option_count`, `top_imported_result`,
  and the dashboard can:
  - import visible provider search results in batch
  - promote a selected imported option directly into `query_seed_result_captured`
  without retyping URL/title/snippet by hand.
- the same upstream lane is no longer just an embedded field in `Current Search Result Capture`:
  it now has a sovereign imported-options review sequence with
  `current_search_query_result_option_row` and `search_query_result_option_summary`.
- the research loop also recognizes that state explicitly:
  when imported options exist, the next operator job becomes `PROMOTE_IMPORTED_RESULT` and loop
  status becomes `awaiting_imported_result_promotion` instead of generic capture.
- the research-loop job queue now consumes that sovereign capture register instead of
  recomputing query-seed state ad hoc, so `READ_OR_DRAFT_REFERENCE`,
  `CAPTURE_SEARCH_RESULT`, and `RESOLVE_REFERENCE_EXCERPT` are aligned across runtime,
  dashboard payload, and loop control.
- latest runtime truth after this slice: `678 passed, 15 warnings`.

# Operational Intelligence Skill Reentry Boundary — Latest

Produced at: 2026-05-07

## Purpose

This file fixes the correct interpretation of the new Operational Intelligence Skill lane.

It exists to prevent future sessions from confusing:

- the already closed dynamic-congruence prompt lane;
- the already closed Phase 2 architecture lane;
- and the bounded registry-first operational-intelligence skill phase that is now closed.

## Current truth

The current executable runtime truth remains:

- suite: `runtime-orchestrator`
- command: `pytest -q`
- result: `739 passed, 15 warnings`
- date: `2026-05-07`

Interpretation:

- the framework is not reopening prior debt;
- the bounded runtime-authority phase of the sovereign knowledge-layer lane is now closed;
- and any further work should be treated as optional deepening, not unresolved implementation debt.

## New governing references

- `operational_intelligence_skill_execution_backlog_latest.md`
- `autonomous_research_loop_master_plan_latest.md`
- `autonomous_research_loop_execution_backlog_latest.md`
- `research_brain_100_percent_completion_plan_latest.md`
- `research_brain_100_percent_completion_backlog_latest.md`
- `report_98_readiness_closure_plan_latest.md`
- `operational_intelligence_skill_phase_closure_latest.md`
- `dynamic_congruence_prompt_closure_refresh_latest.md`
- `dynamic_congruence_phase2_architecture_closure_latest.md`
- `dynamic_congruence_phase2_execution_backlog_latest.md`
- `dynamic_congruence_browser_lane_operational_certification_latest.md`
- `dynamic_congruence_browser_lane_family_expansion_latest.md`

## What is already true

The framework already has usable reasoning distributed across:

- `motor_049`
- `motor_052`
- `motor_053`
- `motor_054`

The bounded phase no longer lacks the previously open closure items:

- approved extraction review now materializes validated pattern / combination promotion registers;
- certification now reaches raw family inputs through a real `Motor035Adapter` layer before the downstream report chain;
- and optional legacy analytical enrichment is now explicitly bounded in runtime outputs rather than left implicit.

What is newly true in that chain:

- `motor_016` now synthesizes its structural package context from `motor_047`, `motor_048`, `motor_053`, and `motor_054` when `motor_034` and `motor_037..046` are absent;
- the mixed-runtime certification helper no longer injects synthetic `motor_014`, `motor_015`, `motor_034`, or `motor_037..046` into the `motor_047 -> motor_048 -> motor_016 -> motor_017 -> motor_027` path for `warehouse`, `manufacturing`, and `building`;
- `motor_016` also synthesizes bounded `output_blocks`, `report_readiness_register`, `claim_permission_register`, and `decision_permission_register` from skill-first surfaces when `motor_014` / `motor_015` are absent;
- `motor_016` also synthesizes bounded `inference_records`, `conflict_register`, `validation_queue`, `next_best_questions`, `evidence_gap_register`, and `opportunity_candidates` from skill-first surfaces when `motor_014` is absent, and the cutover test now proves that `Detailed Validation Questions` remains visibly populated in the package view;
- a second certification layer now proves that the same authority split and visible validation output survive a generic `live-bundle` wrapper built directly from the real case bundle for `warehouse`, `manufacturing`, and `building`, without relying on the family-specific synthetic helper for cutover proof;
- a third certification layer now proves that the same downstream chain survives a `routing-ready live-bundle` wrapper that runs `Motor035Adapter` for real and uses its routing output upstream of `motor_047 -> motor_048 -> motor_016 -> motor_017 -> motor_027`;
- the bounded wrapper context that feeds those cutover certifications is no longer assembled manually in the test file:
  `runtime_orchestrator.zlab_skill.runtime_bridge.build_skill_first_runtime_wrapper_inputs` now derives the
  `__pipeline__`, `__runtime__`, `motor_012.compliance_applicability_case`, and minimal `motor_035` compatibility
  surface from live bundle inputs plus optional `Motor035Adapter` output;
- `patterns` is now promoted to `skill_primary` when the mapped runtime pattern surface is fully covered by the
  registry-first activation register;
- approved extraction review can now produce validated pattern/combination promotion registers for registry review;
- the licensed-research lane now has an executable acquisition -> extraction -> review -> promotion path, still
  bounded by provider credentials and network availability for real provider-authenticated runs;
- the dashboard `/api/live` and `congruence_brain` payload now surface pattern authority, authoritative pattern
  activation, provider session state, and extraction-review / promotion summaries so the new lane is visible even
  before live provider sessions are populated;
- the dashboard now also persists promotion adjudication for extraction-derived pattern/combination proposals, so
  registry-review acceptance can be managed per run before live provider sessions are loaded;
- accepted promotion decisions now materialize a run-level `registry_review_bundle`, available through the live
  dashboard payload and an export endpoint for registry-review handoff;
- accepted promotion decisions now also materialize a `registry_stage_preview`, available through the live payload
  and an export endpoint, so the exact candidate file plan toward `patterns/` and `combinations/` is visible
  before any registry write step exists;
- the dashboard can now materialize that stage preview into a run-scoped local staging directory and persist a
  manifest of written candidate files, still outside the sovereign registry;
- the dashboard can now also materialize a provider-session handoff manifest with per-provider recommended actions,
  profile paths and sample entry URLs, so credentialed live acquisition has an explicit operational handoff surface;
- that provider-session handoff now also carries executable bootstrap and auth-validation commands per provider,
  so the first live login/validation step is operationally defined before credentials are loaded;
- the provider-session lane now also supports `institutional_gateway` access routing with a shared university
  session profile and provider-specific validation URLs, so licensed access no longer assumes direct provider login;
- `Scopus` has now been validated successfully through the UTP proxy path in the persistent licensed profile;
- a new governed fallback lane now exists for provider-hardblocked full text: local licensed PDFs can be ingested
  into manifest -> extraction_review -> promotion_review without pretending automated provider fetch succeeded;
- that local-PDF fallback now also has a scaffolding helper that generates `.metadata.json` and `.extraction.json`
  sidecars for every PDF in an intake directory before batch ingestion;
- that same local-PDF lane can now auto-generate a governed `auto_draft` extraction payload from PDF text plus
  metadata when `.extraction.json` is absent, map registry-backed pattern/combination candidates, and persist the
  generated draft back to disk for operator adjudication or modification;
- a new `Scopus discovery export -> candidate PDF queue` lane can now turn discovery metadata into ranked candidate
  documents, expected PDF filenames, inbox sidecars, and reviewable `auto_draft` pattern/combination proposals before
  the PDF is downloaded;
- that discovery-export queue is now provider-generic for `Scopus`, `IEEE`, `Elsevier`, and `Springer`, while the
  older `Scopus`-named entrypoint remains only as a compatibility alias;
- the dashboard now consumes that discovery queue natively, with run-scoped candidate decisions, candidate edits,
  and article-reference records so operators can approve/reject/modify from UI instead of from files only;
- the dashboard now also batch-reads references for discovery candidates already accepted for follow-up and
  materializes a persistent `accepted_discovery_candidate_bundle`, so approved literature candidates can be
  handed off as a run-level bundle instead of staying as isolated row decisions;
- a further certification layer now exists from raw family inputs through `Motor035Adapter`, `motor_049..054`,
  and the downstream report chain;
- `motor_014` / `motor_015` optionality is now explicit in runtime outputs through bounded legacy-enrichment state flags;
- the sovereign pattern activation register now emits the prompt-literal activation ladder and bounded falsification
  fields instead of a flat placeholder state;
- the strategic-regression correction is now partially closed in runtime:
  `conditional_structural_intelligence` no longer dies in the final client-facing package, and `motor_016`
  now renders bounded strategic fields such as wrong-variable risk, hidden boundary error, invalid-comparison
  risk, dominant loss logic, strategic nuggets, and fallback conditional pathways even when local claim closure
  remains blocked;
- the chart layer is now aligned with that same doctrine:
  `motor_018` can render fair-comparison, cross-layer, and cost-driver charts from `executive_thesis`
  itself when the strategic thesis is already bounded but some older registers are still sparse, preventing
  the PDF from sounding strategic in prose while going mute in visuals;
- the visible thesis body is now aligned too:
  `motor_016` renders challenger hypotheses, differentiated evidence packs, and a wider gold-nugget set
  directly inside `Executive Structural Thesis`, so the report body no longer hides multi-lane strategic
  reasoning behind a mostly single-primary surface;
- the adjacent executive sections are now aligned to that same spine:
  `motor_016` binds `financial exposure`, `peer comparison`, and visible `TAD` actions back to
  the same evidence packs and linked thesis lanes, so those sections no longer behave like
  structurally disconnected side-panels;
- the peer lane is now also materially deeper, not only better rendered:
  `competitive_comparison.py` emits peer requirements, candidate peer frames, plausible
  better-practice deltas, and an explicit no-superiority block reason across warehouse,
  cold-chain, building, and manufacturing archetypes, and `motor_016` surfaces those
  structures visibly in the client-facing peer section;
- the chart lane now also has an explicit strategic-value hierarchy:
  `motor_018` emits per-chart strategic score/tier/reason plus a run-level
  `chart_strategic_value_register`, and chart emission order is now governed by thesis value
  rather than only by construction order;
- that chart doctrine now also controls the main visible surface:
  `motor_016` suppresses `decorative_risk` charts from body sections once enough
  `thesis_critical` / `strategic_support` charts already carry the report logic, and emits
  `chart_strategic_surface_policy_register` plus `chart_strategic_surface_summary` so the
  demotion remains auditable;
- the first `Wave 3` slice is now also live in the thesis surface:
  `gold_nuggets.py` now emits theme-aware strength and priority metadata, `executive_thesis.py`
  now seeds `wrong_problem_frame` first when present and then selects a diversified nugget set by
  theme/strength rather than raw arrival order, and the visible ceiling now widens to a bounded `8`;
- the second `Wave 3` slice is now also live in the action surface:
  `strategic_tad.py` now emits richer decision-front semantics, `executive_thesis.py` now carries
  those fields into `top_actions`, and `motor_016` now renders `Decision Front / Trigger Family /
  Financial Exposure / No-Go Class` directly in the visible TAD section;
- the third `Wave 3` slice is now also live in the correlation surface:
  `congruence_engine.py` now enriches contradiction rows with supporting-correlation and pressure
  sidecars plus a `correlation_constellation_score`, `executive_thesis.py` now emits a bounded
  `correlation_constellation_register` with a conflict-derived fallback when the upstream graph is
  sparse, and `motor_016` now renders `Correlation Constellation Signals` directly in the visible
  thesis instead of collapsing back to one contradiction line;
- the next report-quality slice is now also live in the contamination / density surface:
  `motor_016._build_case_adaptation_memo(...)` now computes an explicit structural-diversity score,
  emits a `diversity_register`, and blocks publication when a case is formally populated but still
  too flat across sources, decision fronts, scenarios, and bottleneck variables; comparable-case
  blocking now also distinguishes high-value divergence from merely cosmetic divergence.
- the remaining `Wave 4` chart closure slice is now also live in the render surface:
  `motor_018.py` now binds each chart to contradiction / hypothesis / nugget logic explicitly, and
  `motor_016._apply_chart_strategic_surface_gate(...)` can demote low-value body charts into a
  governed appendix surface instead of only suppressing them outright.
- the first `Wave 5` surface-density slice is now also live:
  `motor_016` applies a governed section-density gate after empty-section fallback, emits
  `section_density_surface_policy_register` plus `section_density_surface_summary`, and can demote
  thin support sections to appendix while respecting both the protected strategic spine and the
  active render contract / visible outline.
- the second `Wave 5` surface-value slice is now also live:
  `motor_016` now scores populated sections by strategic surface value, emits
  `section_strategic_surface_policy_register` plus `section_strategic_surface_summary`, and can
  demote low-value optional sections to appendix only when the body already carries enough
  high-value sections and the protected / contract-required surface remains intact.
- the third `Wave 5` surface-redundancy slice is now also live:
  `motor_016` now applies section-level redundancy governance, emits
  `section_redundancy_surface_policy_register` plus `section_redundancy_surface_summary`, and can
  demote strategically optional sections to appendix when they materially overlap the retained
  thesis spine while still respecting the protected / contract-required surface and the minimum
  body floor.
- the fourth `Wave 5` surface-redundancy slice is now also live inside the thesis core:
  `executive_thesis.py` now compacts semantically overlapping gold nuggets, evidence packs,
  thesis-constellation rows, and correlation-constellation rows using concept-aware redundancy rules,
  while keeping `challenger_hypothesis` rows alive unless they are genuinely overlapping rather than
  merely concept-adjacent to the lead thesis.
- the fifth `Wave 5` surface-redundancy slice is now also live in the rendered thesis body:
  `motor_016.py` now applies a visible-surface novelty filter to the `Executive Structural Thesis`,
  emits `thesis_surface_compaction_register` plus `thesis_surface_compaction_summary`, preserves a
  bounded primary nugget even when it sharpens the thesis spine, and prevents distinct correlation
  lanes from disappearing merely because a related tariff or denominator nugget is already visible.
- the sixth `Wave 5` thesis-tone slice is now also live in the rendered body:
  `motor_016.py` now emits `thesis_surface_readout_register` and renders a bounded `Strategic Reading`
  block inside `Executive Structural Thesis`, so the client-facing surface leads with governed
  strategic interpretation rather than only a metadata-style thesis inventory.
- the seventh `Wave 5` body-tone slice is now also live across adjacent sections:
  `motor_016.py` now emits `section_surface_readout_register` and opens the visible
  finance / peer / TAD sections with bounded strategic readouts, so the report does not
  regain checklist tone immediately after the thesis block.
- the eighth `Wave 5` body-surface slice is now also live after redundancy gating:
  `motor_016.py` now emits `section_surface_inventory_profile` /
  `section_surface_inventory_state` and the package now carries
  `section_inventory_surface_policy_register` plus `section_inventory_surface_summary`,
  so registry-like optional sections can move to appendix without weakening the strategic spine.
- the ninth `Wave 5` contract-semantics slice is now also live:
  `motor_016.py` no longer protects the whole outline as if it were hard-required; the final package now
  separates `required_body_sections`, `preferred_body_sections`, and `resolved_body_sections`, and records
  `demoted_preferred_body_sections` when body-surface governance compacts optional planned sections away.
- the tenth `Wave 5` contract-spine slice is now also live:
  `render_section_contract.py` now defines a leaner hard-required structural-first body spine, so
  `Scenario Space`, `Conditional Redesign Pathways`, `What Not To Do Yet`, and
  `Claim Permission Matrix` can leave the body without becoming render-contract failures.
- that render doctrine is now acceptance-certified all the way through `motor_017` / `motor_027`:
  cutover tests now read the actual rendered `.tex` chapters and prove that the final package preserves
  wrong-variable / wrong-denominator / fair-comparison language, keeps bounded prohibitions visible,
  and still embeds chart assets end-to-end when the report package carries them;
- the final `RB100-08` review is now also explicit and honest:
  the expanded research-brain target still cannot be certified as `100%` because the rich-source
  `100+` path is still policy-first rather than acceptance-proven, and the upstream research lane
  still depends too heavily on `window.prompt` glue in core transitions;
- `building` now also promotes to `skill_primary`, so the multi-family cutover is no longer split between
  warehouse/manufacturing promotion and building shadow;
- manufacturing now has prompt-literal sovereign pattern coverage for `process_load_vs_waste`,
  `maintenance_hidden_value_driver`, and `procurement_vs_lifecycle_cost`, plus a dedicated acceptance bundle;
- `motor_024` now applies a registry-backed `ReportDiversityValidator` to live report output and blocks
  template-contaminated outputs through preflight;
- prompt section 20 now has explicit structure-equivalence files under `runtime-orchestrator/zlab_skill/*.yaml`;
- the dashboard can now merge staged candidate files into the sovereign registry root after run-level review;
- the dashboard can now also regenerate reference-backed extraction/promotion bundles directly from accepted discovery references;
- those reference-backed promotions now merge into the normal promotion-review circuit, so they can be adjudicated,
  staged and merged like any other registry candidate;
- promotion rows can now also be modified directly from the dashboard before stage/merge, with run-scoped edit
  persistence that propagates into promotion review, registry review bundles, staged candidate files and merge payloads;
- saved article references can now also be modified directly from the dashboard, including manual excerpt/visible-text
  curation; `manual_text_enriched` references count as enriched reference state and feed the reference-backed promotion loop;
- the dashboard can now also create a manual licensed article candidate end-to-end and feed it directly into
  `accepted refs -> reference-backed promotions` without requiring an import file or a PDF;
- accepted references with enriched visible text now also auto-refresh the `reference-backed promotions` manifest on
  create/read/edit and batch-read flows, so the manual refresh endpoint becomes an explicit re-run tool rather than
  a required happy-path step;
- current full-suite truth after the prompt-literal closure plus manual-article creation is now `606 passed, 15 warnings`.
- a new optional-deepening code slice is now active:
  `asset_context_vector` plus latent-combination generation and admissible latent-combination review are available
  in runtime and dashboard payloads, while the existing registry-exact combination lane remains intact.
- that same slice now also exposes first-pass latent-combination clusters in dashboard state and allows
  run-level adjudication of admissible latent combinations through the existing combination decision endpoint/store.
- the latent-combination score now uses richer runtime signals:
  active-pattern evidence state, `source_basis` richness, and context-alignment signals
  instead of only layer count plus generic context presence.
- latent clusters are now actionable from dashboard:
  batch adjudication and `suppress generic template` both persist through the existing
  combination decision store and apply to all admissible candidates in the selected cluster.
- latent clusters now also support real run-scoped `split` and `merge` overrides through a dedicated
  `latent-cluster-overrides` store; cluster recomputation happens inside `dashboard._congruence_brain_activity`
  without mutating the base latent candidate generator.
- the dashboard now renders override metadata (`override_state`, `source_cluster_ids`, optional
  `cluster_label`) and exposes `Split cluster` / `Merge cluster` actions directly in `Congruence Brain`.
- the lane now also builds a sovereign `knowledge_atom_register` plus `source_coverage_summary`
  from licensed extraction records, discovery candidates and reference-backed extraction manifests.
- latent-combination synthesis can now expand its `pattern pool` from research-supported atoms even when
  the extra pattern is not already active in runtime, which makes the pool less dependent on the narrow
  active-pattern surface.
- latent candidates now expose `supporting_atom_ids`, `supporting_document_refs`,
  `supporting_provider_keys`, `knowledge_atom_count`, and the score now includes
  `knowledge_atom_support_score` plus `source_diversity_score`.
- the dashboard now renders `Research Coverage` and per-candidate atom/document support inside
  `Congruence Brain`.
- the lane now also exposes a sovereign `combination_search_gap_record` that decides whether
  the current latent pool is still shallow because the framework under-investigated or whether
  coverage is already strong enough to review a thin pool cautiously.
- `Congruence Brain` now renders `Combination Search Gap` with `search_status`, `severity`,
  `gap_flags`, and `recommended_actions`.
- the lane now also formalizes a sovereign `research_campaign_record` plus
  `source_family_coverage_register`, so the dashboard can show campaign status,
  touched vs missing source families, and next source-expansion actions.
- `Congruence Brain` now renders `Research Campaign` and one card per source family
  with coverage state, provider touch, document counts, and knowledge-atom depth.
- the lane now also exposes a sovereign `research_campaign_trigger_register` plus
  persisted `research-campaign-triggers/<run_id>.json`, so each source family can queue a
  real `deeper_search` action with reason, provider targets, and expected document/atom delta.
- `Congruence Brain` now renders `Trigger deeper search` per source family and the dashboard
  persists the queue through `POST /api/source-family-trigger`.
- `Congruence Brain` now also exposes a sovereign `combination_review_sequence_register`,
  `current_combination_review_row`, and `combination_review_queue_summary`, so combinations can be
  reviewed one at a time instead of as an open wall.
- the dashboard now supports `POST /api/combination-edit` with run-scoped `combination-edits/<run_id>.json`
  and auto-closes a modified combination as `needs_review`, advancing the sequential queue.
- the dashboard now also supports run-scoped `combination-review-controls/<run_id>.json` with
  `deferred_combination_ids` and `batch_size`, exposed through `POST /api/combination-review-control`.
- `Current Combination Review` now supports `Defer` and `Set batch size`, so the reviewer can skip
  and reorder pending combinations without turning that skip into a truth or claim decision.
- the lane now also exposes a sovereign `combination_follow_on_research_register`, and `defer`,
  `modify`, or `needs_review` decisions can automatically queue research-campaign triggers against
  the right source families.
- `Current Combination Review` now shows follow-on research families/flags for the active combination,
  and the runtime persists those auto-queued trigger rows in the existing
  `research-campaign-triggers/<run_id>.json` store.
- the lane now also exposes a sovereign `combination_follow_on_execution_manifest_register` plus
  `current_combination_follow_on_execution_manifest`, so each active combination can yield concrete
  `source_family + provider_targets + query_families + document/atom delta` execution rows.
- the dashboard now supports `POST /api/combination-follow-on-manifest-materialize` and persists those
  execution plans in `combination-follow-on-manifests/<run_id>.json`.
- current full-suite truth after the execution-manifest slice is now `628 passed, 15 warnings`.
- the autonomous-research-loop deepening lane is no longer only documentary:
  it now has sovereign runtime modules for loop policy, job queue, loop state, and loop controller;
  the dashboard persists `research-loop-state`, `research-loop-events`, `research-loop-jobs`,
  and `research-loop-metrics` per run; and `Congruence Brain` now renders `Research Loop State`
  with `current_research_job`, loop metrics, and stop-condition state.
- the draft-resolution slice of that loop is now also started in code:
  `query_seed_draft` references expose prefilled resolution context, and resolving them can now
  update article identity (`title`, `doi`, `URL`) and auto-accept the discovery candidate for
  reference use in the same step.
- the atom-refresh / rerank slice is now also started in code:
  enriched references now produce persisted `knowledge_atom_refresh_summary` and
  `combination_rerank_summary` records per run, and `Congruence Brain` renders that
  closed-loop evidence directly.
- the source-depth-enforcement slice is now also started in code:
  the latent-pool floor is no longer flat, `combination_gap_analyzer` now inspects source-family depth and
  high-priority-family coverage, the loop snapshot now emits a sovereign `research_depth_enforcement_record`,
  and the stop gate only allows controlled saturation when that depth gate is satisfied or explicitly strong enough.
- the stop-condition-and-saturation slice is now also started in code:
  operator control is now persisted per run under `research-loop-controls/<run_id>.json`,
  the loop can now be `paused_by_operator`, `stopped_by_operator`, or `stopped_by_saturation`,
  and those states now travel with explicit reasons in payload instead of being inferred from refresh side effects.
- the dashboard-control-room slice is now also started in code:
  the operator can now advance the loop, pause/resume/stop it, force a rerank, and mark a source family exhausted
  from `Congruence Brain`, and those actions now hit sovereign runtime endpoints instead of remaining manual UI conventions.
- the stronger `research_brain_100_percent_completion` lane is now also started in code:
  search-result intake accepts structured records / JSON arrays as well as legacy packet blocks, and imported
  result options now carry explicit `import_format` provenance through the runtime lane.
- imported-result review in that same lane now also runs at option granularity instead of only candidate granularity,
  and the dashboard can promote the current imported option directly when the runtime already knows which option
  is active, reducing another piece of `window.prompt` glue without weakening epistemic states.
- the same lane now also has its first floor-doctrine hardening slice:
  `20` survives only as a narrow `bootstrap_floor_exception`, while normal campaign depth now resolves to the
  `50+` doctrine and both gap/depth records expose the floor-policy state and reason explicitly.
- the loop now also has dedicated family-level acceptance bundles for `warehouse`, `manufacturing`, and `building`,
  so the new deepening lane is no longer certified only by unit slices or dashboard wiring.
- those bundles now prove three negative guards that should not be reopened casually:
  `query_seed_draft` never counts as evidence, an `incomplete_under_investigated` run cannot stop,
  and duplicate latent-combination rows cannot fake the `50+` floor.
- research-loop metrics and `combination_gap_analyzer` now deduplicate by `combination_id` before enforcing
  floor sufficiency or stop policy, while still exposing raw row counts for audit.
- the dashboard now also treats `query_seed_draft` resolution as a sequential operator workflow:
  `Current Reference Draft Review` presents one pending draft at a time and advances to the next on refresh,
  instead of leaving query-seed resolution as a flat unprioritized article-reference list.
- that same draft-resolution lane is now packet-based:
  the operator can resolve a draft through a single structured packet instead of a long prompt chain,
  and the runtime handles the same downstream atom refresh / rerank loop through the new
  `article-reference-resolve-packet` path.
- the same lane now also supports packet-based batch resolution of visible drafts:
  `Current Reference Draft Review` can resolve multiple compatible `query_seed_draft` rows in one step,
  and the queue advances by multiple items without inventing evidence or bypassing the same rerank path.
- that batch path is now source-aware rather than positional:
  the framework computes a sovereign `reference_resolution_batch_plan` that prefers same-provider
  drafts, then same-`source_family` drafts, before falling back to a mixed pending batch.
- the query-seed lane now also externalizes two runtime surfaces that were still too
  dashboard-centric:
  `provider_query_seed_materializer.py` now owns deterministic `queryseed-*` construction from
  follow-on provider query templates, and `research_query_runner.py` now owns the run-visible
  `search_result_capture_register` / `search_result_capture_sequence`.
- `Congruence Brain` now renders that intermediate lane explicitly as `Current Search Result Capture`,
  so the operator sees `needs_draft`, `capture_search_result`, and `resolve_reference_excerpt`
  as sovereign queue states before excerpt enrichment.
- the same lane now also emits a sovereign `search_query_execution_register` with provider-specific
  search packets, query variants, and capture templates, so the search step is no longer just
  implied by hints scattered across dashboard cards.
- that execution lane now also supports two run-scoped operational artifacts:
  a persisted `search-query-execution-manifests/<run_id>.json` plan and a batch search-result
  capture endpoint, so `provider search -> source hit context` can advance in groups instead of one
  candidate at a time.
- excerpt resolution now hydrates missing `source_url` / title / notes from the previously captured
  source-hit context, so the operator can promote a draft with just the real excerpt when the URL
  is already known.
- the batch resolver now has a captured-context branch:
  `reference_resolution_batch_plan` emits `captured_ready` and
  `captured_quick_packet_template`, so compatible drafts can resolve in groups without repeating
  already captured URLs.
- the search-execution lane now has a second upstream artifact:
  `search-query-result-imports/<run_id>.json`, which stores visible provider results per
  candidate before a final source hit is chosen.
- pending `queryseed-*` rows now expose imported-option counts and top imported results, and the
  dashboard can promote a selected imported option directly into
  `query_seed_result_captured` without retyping URL/title/snippet.
- that imported-options state is now a sovereign review lane in its own right:
  `current_search_query_result_option_row` / `search_query_result_option_summary` surface the next
  imported option review one at a time.
- the loop also treats that as a distinct operator task:
  `PROMOTE_IMPORTED_RESULT` with loop status `awaiting_imported_result_promotion`.

## Closure interpretation

The correct interpretation now is:

1. the bounded runtime-authority phase is closed;
2. the current lane is not blocked by unfinished mandatory work;
3. any further work should be opened as a separate optional deepening phase;
4. the four most natural optional frontiers are:
   - provider-authenticated literature population using `Playwright`
   - registry growth beyond the current foundational packs and approved promotion seeds
   - certification further upstream than the current raw-input + `Motor035Adapter` chain
   - deeper replacement of optional `motor_014` enrichment beyond what the cutover path requires
5. the highest-value optional frontier is now formalized separately in:
   - `latent_combination_research_expansion_plan_latest.md`
   This is the plan for teaching the framework to perform broader multi-source investigations and to surface
   large pools of latent cross-layer combinations before human adjudication.

## What not to do

Do not:

- rewrite the whole framework from scratch;
- delete `motor_049`, `motor_052`, `motor_053`, or `motor_054` outright;
- bypass existing claim, artifact, or chart consistency guards;
- or let licensed literature act as local case truth.

## First-slice status

The initial scaffold is now started:

- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/`
- `runtime-orchestrator/zlab_skill/registry/`
- `runtime-orchestrator/tests/test_zlab_skill_registry.py`
- `runtime-orchestrator/tests/test_operational_intelligence_dashboard_live_contracts.py`
- `runtime-orchestrator/tests/test_operational_intelligence_dashboard_congruence_brain.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/provider_sessions.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/playwright_profiles.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/licensed_acquisition.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/research_manifest.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/licensed_playwright_fetch.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/extraction_schema.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/extractor.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/extraction_review.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/combination_engine.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/adjudication_engine.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/validator_engine.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/memory_scope.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/memory_engine.py`
- `runtime-orchestrator/src/runtime_orchestrator/zlab_skill/runtime_bridge.py`
- `runtime-orchestrator/zlab_skill/registry/memory_policies/`
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_052.py` additive registry-first bridge fields
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_053.py` additive registry-first financial exposure fields
- `runtime-orchestrator/src/runtime_orchestrator/adapters/motor_054.py` additive registry-first combination/TAD/nugget fields
- `runtime-orchestrator/tests/test_operational_intelligence_skill_cutover.py`

That means the reentry point is no longer “finish the current phase”.
The bounded phase is closed. `motor_014` / `motor_015` are now bounded compatibility enrichers, not chain blockers. If work resumes, it should resume as optional deepening beyond the current `Motor035Adapter`-backed wrapper utility, not as closure repair.

## Latest optional deepening reentry

The newest clean reentry point is:

1. the latent-combination lane already materializes provider-specific follow-on research plans from
   `combination_follow_on_execution_manifest_register`;
2. those manifests already seed deterministic `queryseed-*` discovery candidates into the existing
   discovery/reference/promotions lane;
3. those `queryseed-*` candidates already convert into epistemically safe `query_seed_draft`
   article references instead of pretending to be real article reads;
4. those drafts can now be resolved directly to `manual_text_enriched` through `Resolve draft`,
   capturing a real article URL and a real visible excerpt in one guided step;
5. the next clean slice is therefore no longer draft creation or draft resolution;
6. the next clean slice is to reduce remaining manual copy for providers that already permit visible
   reference reading, ideally pre-filling or capturing the excerpt with less operator typing.

Latest runtime truth at this boundary: `632 passed, 15 warnings`.
2. those manifests already contain `search_surface`, `primary_query`, `pivot_query`,
   `asset_focus_terms`, and `evidence_targets` per provider and `query_family`;
3. those concrete query templates now also seed deterministic `queryseed-*` discovery candidates directly into the
   dashboard lane, so a queued combination can create research leads without opening a separate workflow;
4. those `queryseed-*` candidates now also become `query_seed_draft` article references through the existing
   `Read reference` action, preserving epistemic honesty while giving the operator a provider-specific search brief to
   refine;
5. those `query_seed_draft` references can now also be resolved directly to `manual_text_enriched` through
   `Resolve draft`, capturing a real article URL and real visible excerpt in one guided step;
6. `Current Reference Draft Review` now also computes a guided `reference_resolution_batch_plan` that prefers
   same-provider + same-`query_family` + same-evidence-intent batches before looser source-family fallback;
7. that batch plan now exposes `query_families`, `evidence_targets`, and `batch_reason`, so batch resolution remains
   coherent with the research question instead of just grouping by provider;
8. `query_seed_draft` resolution packets now also preserve provider-aware hints such as `launch_url`,
   `search_surface`, `execution_hint`, `primary_query`, and `pivot_query`, and they render those hints directly in the
   editable packet before `URL:`;
9. the next clean slice is therefore no longer “make batch resolution source-aware” or “preserve provider hints into
   the packet”, because both are now done;
10. `query_seed_draft` now also supports a shorter `Resolve excerpt` path that only asks for the real article URL,
    visible excerpt, and optional notes, while still using the same sovereign edit/rerank machinery underneath;
11. the next clean slice is therefore no longer “add a shorter path than packet-based resolution”, because that is
    now done;
12. batch resolution now mirrors the same split:
    - short `Resolve source batch`
    - rich `Resolve full source batch`
    both over the same sovereign endpoint/path;
13. the next clean slice is therefore no longer “give batch the same short-vs-full split as single draft resolution”,
    because that is now done;
14. a new intermediate `Capture result` step now exists inside the same draft lane, storing real article URL plus
    visible result title/snippet while keeping the reference in `query_seed_draft`;
15. the next clean slice is therefore no longer “create an intermediate search-result capture step before excerpt
    resolution”, because that is now done;
16. the next clean slice is to reduce the remaining manual copy even further for providers that already allow visible
    reference reading, ideally by carrying more context directly from that captured result into the final excerpt step.

Latest runtime truth at this boundary: `678 passed, 15 warnings`.

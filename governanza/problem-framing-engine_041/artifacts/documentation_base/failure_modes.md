# Failure Modes — Problem Framing Engine

Motor ID: motor_041

## expected_failures
- `SYMPTOM_LOCK_IN`: el motor repite el síntoma original y no lo corrige.
- `FALLBACK_ERASURE`: el fallback de `motor_051` se usa pero se pierden capas o evidencia_needed.
- `SOLUTION_LEAKAGE`: el reencuadre ya recomienda solución o CAPEX.
- `GENERIC_REFRAME`: el problema reformulado queda tan abstracto que no discrimina nada.

## downstream_risk
- empujar strategy sobre framing equivocado;
- sesgar financial exposure y evidence maturity;
- producir una tesis ejecutiva elegante pero mal planteada.

# Optional MCP Sources

Potential future sources:

- Local document index server for phase files and report versions.
- Internal source registry for evidence provenance.
- PDF/OCR parsing service for difficult scanned documents.
- Developer docs bridge for maintaining prompt and schema compatibility.

Integration rule:

Any MCP-returned material must be written into the audit manifest with source identity and retrieval timestamp before it influences an audit result.

